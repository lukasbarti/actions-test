name: greet

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  greet:
    runs-on: [self-hosted]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Greet the world
        run: echo "Hello, World!"